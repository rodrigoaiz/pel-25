{
  "name": "pel-25",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"webpack --watch --mode development\" \"php -S localhost:8000\"",
    "build": "NODE_ENV=production webpack --mode production",
    "build:staging": "NODE_ENV=staging webpack --mode production",
    "deploy": "./deploy.sh production",
    "deploy:staging": "./deploy.sh staging",
    "deploy:build": "./deploy.sh production --auto-build",
    "deploy:build:staging": "./deploy.sh staging --auto-build",
    "deploy:quick": "./deploy.sh production --no-package",
    "deploy:quick:staging": "./deploy.sh staging --no-package",
    "clean": "rm -rf dist/ deploy-package/ *.tar.gz",
    "serve": "php -S localhost:8000 -t dist/",
    "setup:deploy": "cp deploy.config.example deploy.config && echo '✅ Archivo deploy.config creado. Edítalo con tus datos.'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.7",
    "@tailwindcss/typography": "^0.5.16",
    "clean-webpack-plugin": "^4.0.0",
    "concurrently": "^9.1.2",
    "copy-webpack-plugin": "^12.0.2",
    "css-loader": "^7.1.2",
    "mini-css-extract-plugin": "^2.9.2",
    "postcss-loader": "^8.1.1",
    "style-loader": "^4.0.0",
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.0"
  },
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
  },
  "dependencies": {
    "@fontsource-variable/open-sans": "^5.1.1",
    "@fontsource-variable/roboto-condensed": "^5.1.1",
    "@justinribeiro/lite-youtube": "^1.7.1",
    "bootstrap-icons": "^1.11.3",
    "flowbite": "^3.1.2",
    "fontfaceobserver": "^2.3.0",
    "path": "^0.12.7",
    "postcss": "^8.5.2",
    "tailwindcss": "^4.0.7"
  }
}
