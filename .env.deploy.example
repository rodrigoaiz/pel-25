# Configuración de Despliegue
# Copia este archivo como .env.deploy y configura tus credenciales

# Configuración del servidor de producción
DEPLOY_HOST=tu-servidor.com
DEPLOY_USER=tu-usuario
DEPLOY_PATH=/ruta/completa/a/tu/directorio/web/

# Configuración del servidor de staging (opcional)
STAGING_HOST=staging.tu-servidor.com
STAGING_USER=tu-usuario
STAGING_PATH=/ruta/al/directorio/staging/

# Configuración FTP (alternativa)
FTP_SERVER=ftp.tu-servidor.com
FTP_USERNAME=tu-usuario-ftp
FTP_PASSWORD=tu-password-ftp

# Opciones de rsync
RSYNC_OPTIONS="--delete --exclude='*.map' --exclude='.DS_Store'"

# Notificaciones (opcional)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Backup antes de despliegue (recomendado)
BACKUP_BEFORE_DEPLOY=true
BACKUP_PATH=/backups/pel-25/
